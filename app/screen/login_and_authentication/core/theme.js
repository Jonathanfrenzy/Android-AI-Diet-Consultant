import { DefaultTheme } from 'react-native-paper'

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: '#0bad7c',
    primary: '#0bad7c',
    secondary: '#0bad7c',
    error: '#obad7c',
  },
}
